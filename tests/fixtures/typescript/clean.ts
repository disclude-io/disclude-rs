// Fixture: ordinary TypeScript module with nothing obfuscation-worthy.
export function greet(name: string): string {
    return `hello, ${name}`;
}

export function add(a: number, b: number): number {
    return a + b;
}
