export const CONFIG = {version:1,name:"app",description:"a very long config object that spans a single line for no good reason at all, triggering the long-line INFO signal that we expect the raw pass to emit on non-minified sources whose lines exceed 500 bytes without appearing to be a legitimate minified file that would carry a .min extension, so this comment keeps expanding until we cross the 500-byte threshold comfortably and emit the finding.",flags:{a:true,b:false,c:null},items:["one","two","three","four","five","six","seven"]};
